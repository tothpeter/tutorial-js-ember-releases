<div class="row">
   <div class="large-2 columns">
      <ul class="side-nav">
        {{#each release in model}}
          <li>{{link-to release.tag_name "releases.show" release}}</li>
        {{/each}}
      </ul>
   </div>

  <div class="large-10 columns end">
    {{outlet}}
  </div>
 </div>